{
  "name": "html-traits-on",
  "version": "0.0.0",
  "description": "A trait to listen to events and perform an action or submit a form.",
  "bugs": {
    "url": "https://github.com/jon49/form-subscribe/issues"
  },
  "homepage": "https://github.com/jon49/form-subscribe#readme",
  "main": "index.js",
  "scripts": {
    "start": "npx esbuild --watch --outfile=docs/index.js --servedir=docs --format=iife index.js",
    "build": "npx esbuild --minify --format=iife index.js > index.min.js && npx esbuild --format=iife index.js > docs/index.js"
  },
  "author": "Jon Nyman",
  "license": "MIT",
  "keywords": [
    "form",
    "html",
    "subscribe",
    "eventing",
    "simple",
    "html-traits",
    "built-in"
  ],
  "devDependencies": {
    "esbuild": "^0.25.6"
  }
}
